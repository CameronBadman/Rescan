# Implementation verification — 2026-09-06

- Maven `verify`: **32 Java tests passed**, none skipped, using real libSQL and Redis test containers. Coverage includes the 4,000-document manifest, asynchronous verification and pagination; rejection at 4,001; owner isolation; ten concurrent claims; duplicate completion; expired worker/verifier leases; retries; deletion fencing; transaction rollback; migration checksums; ambiguous HTTP commits; format routing; mixed PDFs; encrypted PDFs; and scaling decisions.
- Python reading-order suite: **2 tests passed**.
- API, Lambda-adapted API, controller, development auth and worker container images built successfully with Java 21. Host Maven tests used JDK 25 with Java 21 compilation target. Docker's socket was unavailable, so local containers ran through rootless Podman.
- Both Terraform roots passed provider initialization, formatting checks and validation. GitHub Actions workflows passed actionlint 1.7.7. No AWS plan/apply or hosted workflow execution was performed.
- Complete Compose staging passed signed S3 uploads, authenticated API submission, asynchronous verification, Redis delivery, Turso-compatible metadata updates, JSON result retrieval and deletion. A five-document batch processed native TXT, PNG, JPEG, TIFF and a three-page mixed PDF; its page extraction order was Tika / ViT / Tika.
- Packaged Java-to-Python image parsing passed with networking disabled and the non-root worker user.
- Authenticated deletion immediately blocked result API access. After advancing only the two synthetic jobs' deletion timestamps past the capability-expiry window, the controller removed their records and all original/result S3 versions. Source fixtures remain reproducible with `scripts/SmokeFixtures.java`.
- The [small-model comparison](ocr-small-benchmark.json) records six synthetic fixtures: 30.30 seconds and approximately 1.08 GiB OCR-process peak RSS for the small reused model, versus 143.39 seconds and 2.44 GiB for the previous pipeline. Mean normalized character error rate regressed from 2.17% to 7.23%. Different CPU allocations and model reuse mean this is a pipeline comparison, not an isolated model speed comparison. The complete staging worker's observed cgroup memory peak was 2,234,589,184 bytes (about 2.08 GiB) across the smoke tests, under its 4 GiB limit; this does not establish worst-case memory at maximum page limits.

Not executed: hosted Turso/Upstash acceptance (credentials required), deployment to AWS, actual Fargate scaling/task protection, Lambda/API Gateway integration, approved release/rollback in GitHub, a 4,000-document OCR throughput run, or accuracy evaluation on real customer resumes. The 4,000-document integration test mocks S3 HEAD responses; separate end-to-end smoke tests use LocalStack S3 and real local libSQL/Redis. `worker_profile_verified` remains false by default: review real-format accuracy and memory before activation. See [operations](operations.md) for AWS acceptance and [deployment](deployment.md) for prerequisites.
