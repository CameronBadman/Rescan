# Baked into the static build. The API key is visible to anyone who loads the
# page; it is the frontend's own key and can be rotated independently.
NEXT_PUBLIC_RESCAN_API_URL=http://localhost:8080
NEXT_PUBLIC_RESCAN_API_KEY=
